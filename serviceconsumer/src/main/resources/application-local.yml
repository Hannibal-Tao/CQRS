logging:
  level:
    org.apache.zookeeper.ClientCnxn: WARN
spring:
  cloud:
    zookeeper:
      connect-string: localhost:2181,localhost:2182,localhost:2183
  data:
    mongodb:
      host: localhost
      port: 27017
      database: "events"

mongo:
  host: localhost